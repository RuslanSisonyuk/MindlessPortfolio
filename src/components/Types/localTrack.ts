export interface LocalTrack {
    id: string
    title: string
    // artist: string
    album: string
    duration: number
    // genre: string
    audioUrl: string
    artworkUrl?: string
    // tags: string[]
    dateAdded: string
    // fileSize?: number
}